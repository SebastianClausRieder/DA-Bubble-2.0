import { Routes } from '@angular/router';
import { MasterLoginSignUpComponent } from './master-login-sign-up/master-login-sign-up.component';

export const routes: Routes = [
    { path: '', component: MasterLoginSignUpComponent }
];